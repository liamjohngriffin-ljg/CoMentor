# CoMentor

Streamlit app for executive communication analysis (transcription + coaching report).

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
export OPENAI_API_KEY="YOUR_KEY"
streamlit run app.py
